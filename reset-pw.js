const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const hashed = await bcrypt.hash('admin123', 10); await prisma.user.update({ where: { username: 'admin_simoketawang' }, data: { password: hashed } }); console.log('Reset OK'); } main().finally(() => prisma.$disconnect());
